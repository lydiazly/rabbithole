[project]
name = "snake"
version = "0.1.0"
description = "A minimal terminal snake game"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
snake = "snake:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
